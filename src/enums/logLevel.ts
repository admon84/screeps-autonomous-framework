/**
 * The `LogLevel` enum is used by the log utility to color console messages.
 */

export enum LogLevel {
  Alert = 1,
  Success = 2,
  Error = 3,
  Warn = 4,
  Info = 5,
  Debug = 6,
  Verbose = 7
}
